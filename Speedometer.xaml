<Window x:Class="exui_wpf.Speedometer"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:exui_wpf"
        Title="Speedometer" Height="300" Width="300" MinHeight="150" MinWidth="150"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent" Topmost="True"
        MouseLeftButtonDown="Window_CustomMouseDown"
        MouseMove="Window_CustomMouseMove"
        MouseLeftButtonUp="Window_CustomMouseUp"
        x:Name="SpeedWindowRoot">

    <Window.Resources>
        <DropShadowEffect x:Key="TextShadow" BlurRadius="6" Color="Black" Opacity="1.0" ShadowDepth="0"/>
        
        <Style x:Key="CenterLabelStyle" TargetType="TextBlock">
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="Effect" Value="{StaticResource TextShadow}"/>
            <Setter Property="RenderTransform"><Setter.Value><TranslateTransform X="-12" Y="-12"/></Setter.Value></Setter>
        </Style>

        <Style x:Key="WhiteGlowStyle" TargetType="TextBlock" BasedOn="{StaticResource CenterLabelStyle}">
            <Setter Property="Effect">
                <Setter.Value><DropShadowEffect BlurRadius="20" Color="White" Opacity="1.0" ShadowDepth="0"/></Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RedGlowStyle" TargetType="TextBlock" BasedOn="{StaticResource CenterLabelStyle}">
            <Setter Property="Effect">
                <Setter.Value><DropShadowEffect BlurRadius="20" Color="#FF3B30" Opacity="1.0" ShadowDepth="0"/></Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Window.Style>
        <Style TargetType="Window">
            <Setter Property="ResizeMode" Value="CanResizeWithGrip"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding ProgramState.IsEditorMode, FallbackValue=True}" Value="False">
                    <Setter Property="ResizeMode" Value="NoResize"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Style>

    <Grid>
        <Border x:Name="EditorModeCanvasTint">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Background" Value="Transparent"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ProgramState.IsEditorMode, FallbackValue=True}" Value="True">
                            <Setter Property="Background" Value="#1A000000"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
        </Border>

        <Viewbox Stretch="Uniform">
            <Border x:Name="HUDBorder" Width="300" Height="240" CornerRadius="20" BorderThickness="2">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Background" Value="#CC111112"/>
                        <Setter Property="BorderBrush" Value="#64D2FF"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ProgramState.IsEditorMode, FallbackValue=True}" Value="False">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="BorderBrush" Value="Transparent"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>

                <Canvas Width="300" Height="240">
                    
                    <Path Data="M 56.0,184.2 A 100,100 0 0 1 226.6,85.7" StrokeThickness="2.5" StrokeDashArray="3 2" Opacity="0.85">
                        <Path.Stroke>
                            <LinearGradientBrush StartPoint="0,1" EndPoint="1,0.2">
                                <GradientStop Color="#25FFFFFF" Offset="0.0"/>
                                <GradientStop Color="#AFFFFFFF" Offset="0.5"/>
                                <GradientStop Color="#FFFFFFFF" Offset="0.85"/>
                                <GradientStop Color="#FF3B30" Offset="1.0"/>
                            </LinearGradientBrush>
                        </Path.Stroke>
                        <Path.Effect>
                            <DropShadowEffect BlurRadius="6" Color="Black" Opacity="1.0" ShadowDepth="0"/>
                        </Path.Effect>
                    </Path>

                    <Canvas Canvas.Left="32.5" Canvas.Top="192.8">
                        <TextBlock Text="0" FontSize="16" FontWeight="Bold" Foreground="#8E8E93" Style="{StaticResource CenterLabelStyle}"/>
                        <TextBlock Text="0" FontSize="16" FontWeight="Bold" Foreground="#FFFFFF" Style="{StaticResource WhiteGlowStyle}" Opacity="{Binding SmoothedSpeed, ElementName=SpeedWindowRoot, Converter={x:Static local:Speedometer.Glow}, ConverterParameter=0}"/>
                        <TextBlock Text="0" FontSize="16" FontWeight="Bold" Foreground="#FFFFFF" Style="{StaticResource CenterLabelStyle}" Opacity="{Binding SmoothedSpeed, ElementName=SpeedWindowRoot, Converter={x:Static local:Speedometer.Glow}, ConverterParameter=0}"/>
                    </Canvas>

                    <Canvas Canvas.Left="25.2" Canvas.Top="143.8">
                        <TextBlock Text="30" FontSize="16" FontWeight="SemiBold" Foreground="#8E8E93" Style="{StaticResource CenterLabelStyle}"/>
                        <TextBlock Text="30" FontSize="16" FontWeight="SemiBold" Foreground="#FFFFFF" Style="{StaticResource WhiteGlowStyle}" Opacity="{Binding SmoothedSpeed, ElementName=SpeedWindowRoot, Converter={x:Static local:Speedometer.Glow}, ConverterParameter=30}"/>
                        <TextBlock Text="30" FontSize="16" FontWeight="SemiBold" Foreground="#FFFFFF" Style="{StaticResource CenterLabelStyle}" Opacity="{Binding SmoothedSpeed, ElementName=SpeedWindowRoot, Converter={x:Static local:Speedometer.Glow}, ConverterParameter=30}"/>
                    </Canvas>

                    <Canvas Canvas.Left="37.4" Canvas.Top="95.8">
                        <TextBlock Text="60" FontSize="16" FontWeight="SemiBold" Foreground="#8E8E93" Style="{StaticResource CenterLabelStyle}"/>
                        <TextBlock Text="60" FontSize="16" FontWeight="SemiBold" Foreground="#FFFFFF" Style="{StaticResource WhiteGlowStyle}" Opacity="{Binding SmoothedSpeed, ElementName=SpeedWindowRoot, Converter={x:Static local:Speedometer.Glow}, ConverterParameter=60}"/>
                        <TextBlock Text="60" FontSize="16" FontWeight="SemiBold" Foreground="#FFFFFF" Style="{StaticResource CenterLabelStyle}" Opacity="{Binding SmoothedSpeed, ElementName=SpeedWindowRoot, Converter={x:Static local:Speedometer.Glow}, ConverterParameter=60}"/>
                    </Canvas>

                    <Canvas Canvas.Left="67.3" Canvas.Top="56.3">
                        <TextBlock Text="90" FontSize="16" FontWeight="SemiBold" Foreground="#8E8E93" Style="{StaticResource CenterLabelStyle}"/>
                        <TextBlock Text="90" FontSize="16" FontWeight="SemiBold" Foreground="#FFFFFF" Style="{StaticResource WhiteGlowStyle}" Opacity="{Binding SmoothedSpeed, ElementName=SpeedWindowRoot, Converter={x:Static local:Speedometer.Glow}, ConverterParameter=90}"/>
                        <TextBlock Text="90" FontSize="16" FontWeight="SemiBold" Foreground="#FFFFFF" Style="{StaticResource CenterLabelStyle}" Opacity="{Binding SmoothedSpeed, ElementName=SpeedWindowRoot, Converter={x:Static local:Speedometer.Glow}, ConverterParameter=90}"/>
                    </Canvas>

                    <Canvas Canvas.Left="110.2" Canvas.Top="31.5">
                        <TextBlock Text="120" FontSize="16" FontWeight="SemiBold" Foreground="#8E8E93" Style="{StaticResource CenterLabelStyle}"/>
                        <TextBlock Text="120" FontSize="16" FontWeight="SemiBold" Foreground="#FFFFFF" Style="{StaticResource WhiteGlowStyle}" Opacity="{Binding SmoothedSpeed, ElementName=SpeedWindowRoot, Converter={x:Static local:Speedometer.Glow}, ConverterParameter=120}"/>
                        <TextBlock Text="120" FontSize="16" FontWeight="SemiBold" Foreground="#FFFFFF" Style="{StaticResource CenterLabelStyle}" Opacity="{Binding SmoothedSpeed, ElementName=SpeedWindowRoot, Converter={x:Static local:Speedometer.Glow}, ConverterParameter=120}"/>
                    </Canvas>

                    <Canvas Canvas.Left="159.3" Canvas.Top="25.4">
                        <TextBlock Text="150" FontSize="16" FontWeight="SemiBold" Foreground="#8E8E93" Style="{StaticResource CenterLabelStyle}"/>
                        <TextBlock Text="150" FontSize="16" FontWeight="SemiBold" Foreground="#FFFFFF" Style="{StaticResource WhiteGlowStyle}" Opacity="{Binding SmoothedSpeed, ElementName=SpeedWindowRoot, Converter={x:Static local:Speedometer.Glow}, ConverterParameter=150}"/>
                        <TextBlock Text="150" FontSize="16" FontWeight="SemiBold" Foreground="#FFFFFF" Style="{StaticResource CenterLabelStyle}" Opacity="{Binding SmoothedSpeed, ElementName=SpeedWindowRoot, Converter={x:Static local:Speedometer.Glow}, ConverterParameter=150}"/>
                    </Canvas>

                    <Canvas Canvas.Left="207.0" Canvas.Top="38.8">
                        <TextBlock Text="180" FontSize="16" FontWeight="Bold" Foreground="#802020" Style="{StaticResource CenterLabelStyle}"/>
                        <TextBlock Text="180" FontSize="16" FontWeight="Bold" Foreground="#FF3B30" Style="{StaticResource RedGlowStyle}" Opacity="{Binding SmoothedSpeed, ElementName=SpeedWindowRoot, Converter={x:Static local:Speedometer.Glow}, ConverterParameter=180}"/>
                        <TextBlock Text="180" FontSize="16" FontWeight="Bold" Foreground="#FF3B30" Style="{StaticResource CenterLabelStyle}" Opacity="{Binding SmoothedSpeed, ElementName=SpeedWindowRoot, Converter={x:Static local:Speedometer.Glow}, ConverterParameter=180}"/>
                    </Canvas>
                    
                    <Canvas Canvas.Left="245.8" Canvas.Top="69.7">
                        <TextBlock Text="210" FontSize="16" FontWeight="Bold" Foreground="#802020" Style="{StaticResource CenterLabelStyle}"/>
                        <TextBlock Text="210" FontSize="16" FontWeight="Bold" Foreground="#FF3B30" Style="{StaticResource RedGlowStyle}" Opacity="{Binding SmoothedSpeed, ElementName=SpeedWindowRoot, Converter={x:Static local:Speedometer.Glow}, ConverterParameter=210}"/>
                        <TextBlock Text="210" FontSize="16" FontWeight="Bold" Foreground="#FF3B30" Style="{StaticResource CenterLabelStyle}" Opacity="{Binding SmoothedSpeed, ElementName=SpeedWindowRoot, Converter={x:Static local:Speedometer.Glow}, ConverterParameter=210}"/>
                    </Canvas>

                    <TextBlock Text="MPH" FontSize="9" FontWeight="Black" Foreground="#444446" Canvas.Left="142" Canvas.Top="182"/>

                    <Grid x:Name="NeedleAssembly" Canvas.Left="40" Canvas.Top="40" Width="220" Height="220" Panel.ZIndex="5">
                        <Grid.RenderTransform>
                            <TransformGroup>
                                <RotateTransform CenterX="110" CenterY="110" Angle="-20"/> 
                                <RotateTransform CenterX="110" CenterY="110" Angle="{Binding SmoothedAngle, ElementName=SpeedWindowRoot, FallbackValue=0}"/>
                            </TransformGroup>
                        </Grid.RenderTransform>
                        
                        <Path Data="M 110,105.5 L 2,109.5 L 2,110.5 L 110,114.5 Z" Fill="#FF3B30">
                            <Path.Effect><DropShadowEffect BlurRadius="30" Color="#FF3B30" Opacity="1.0" ShadowDepth="0"/></Path.Effect>
                        </Path>
                        <Path Data="M 110,105.5 L 2,109.5 L 2,110.5 L 110,114.5 Z" Fill="#FF3B30"/>
                        <Path Data="M 110,108.5 L 2,109.8 L 2,110.2 L 110,111.5 Z" Fill="#FFEEED">
                            <Path.Effect><BlurEffect Radius="3"/></Path.Effect>
                        </Path>
                    </Grid>

                    <Border Background="#1C1C1E" CornerRadius="22" BorderBrush="#48484A" BorderThickness="1.5" Width="44" Height="44" Canvas.Left="128" Canvas.Top="128" Panel.ZIndex="10">
                        <Grid>
                            <Ellipse Margin="2" Stroke="#111112" StrokeThickness="1"/>
                            <TextBlock FontFamily="pack://application:,,,/Speedometer;component/seven-segment.ttf#Seven Segment" FontSize="30" FontWeight="Normal" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="12,1,0,0">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="{Binding Telemetry[gear], FallbackValue=3}"/>
                                        <Setter Property="Foreground" Value="#FFFFFF"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Telemetry[gear]}" Value="1">
                                                <Setter Property="Text" Value="N"/>
                                                <Setter Property="Foreground" Value="#FFCC00"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Telemetry[gear]}" Value="0">
                                                <Setter Property="Text" Value="R"/>
                                                <Setter Property="Foreground" Value="#FF3B30"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Telemetry[gear]}" Value="2"><Setter Property="Text" Value="1"/></DataTrigger>
                                            <DataTrigger Binding="{Binding Telemetry[gear]}" Value="3"><Setter Property="Text" Value="2"/></DataTrigger>
                                            <DataTrigger Binding="{Binding Telemetry[gear]}" Value="4"><Setter Property="Text" Value="3"/></DataTrigger>
                                            <DataTrigger Binding="{Binding Telemetry[gear]}" Value="5"><Setter Property="Text" Value="4"/></DataTrigger>
                                            <DataTrigger Binding="{Binding Telemetry[gear]}" Value="6"><Setter Property="Text" Value="5"/></DataTrigger>
                                            <DataTrigger Binding="{Binding Telemetry[gear]}" Value="7"><Setter Property="Text" Value="6"/></DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Grid>
                    </Border>
                    
                </Canvas>
            </Border>
        </Viewbox>
    </Grid>
</Window>